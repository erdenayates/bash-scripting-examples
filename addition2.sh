echo "First cluster's values = "
read -a first_cluster
echo "Second cluster's values = "
read -a second_cluster
echo ${first_cluster[0]},${first_cluster[1]}
echo ${second_cluster[0]},${second_cluster[1]}
